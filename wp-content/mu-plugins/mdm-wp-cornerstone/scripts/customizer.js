 jQuery( function( $ ) {
 	// Logo Width
 	// wp.customize( 'mdm_genesis_tweaks[logo_width]', function( value ) {
 	// 	value.bind( function( to ) {
 	// 		$( '.custom-logo' ).css( {
 	// 			'max-width': to + 'px'
 	// 		} );
 	// 	} );
 	// } );
 });